import React from 'react';
import ReactDOM from 'react-dom/client';
import VesVistaStore from './VesVistaStore';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <VesVistaStore />
  </React.StrictMode>
);